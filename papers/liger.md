---
论文标题: "Unifying Generative and Dense Retrieval for Sequential Recommendation"
方法简称: "LIGER"
中文名称: "生成检索与稠密检索统一"
年份: 2024
发表场所: "预印本"
范围判断: "边界方法"
论文链接: "未报告"
代码链接: "未发现公开代码"
架构图: "../assets/architectures/liger.png"
标签: ["生成检索", "稠密检索", "混合检索", "冷启动"]
---

# LIGER：生成检索与稠密检索统一

> LIGER 将生成式检索与稠密检索结合，以缓解生成模型对冷启动物品和开放物品集合的覆盖不足。

## 方法架构

![方法架构图](../assets/architectures/liger.png)

*图源：原论文，图2。图片使用许可待核实。*

## 研究动机与问题定义

生成式检索能直接生成 SID，但对训练集中未出现或难以生成的物品不够稳健；稠密检索具有更好的开放集合覆盖。LIGER 研究两者的混合推荐流程。

## TL;DR（核心摘要）

`用户历史 → 生成式检索候选 + 稠密检索候选 → 外部融合/排序 → 推荐物品`

模型先用生成式检索产生 SID 候选，再将其与稠密检索结果及冷启动候选合并，并通过编码器相似度进行最终排序。因此，完整 SID 并不能单独决定最终推荐物品，属于边界方法。

## 方法设计速览

| 组成部分 | 具体设计 |
|---|---|
| 使用信息 | 用户序列、SID 表示和稠密用户/物品向量 |
| 信息融入位置 | 生成召回、稠密召回和融合排序 |
| 语义标识构建 | 沿用语义标识编码 |
| 语义标识结构 | 多 token 序列 |
| 生成模型 | 生成式检索模型 |
| 训练方式 | 生成与稠密检索分别训练 |
| 解码方式 | 生成式检索束搜索 |
| 适用场景 | 开放物品、冷启动和混合检索 |

## 方法设计

### 2.1 生成式检索与稠密检索
LIGER 对比两条序列推荐链路：生成式模型根据历史 SID 束搜索目标标识，稠密模型将历史和物品文本编码为向量并进行相似度检索。两者在物品表示、输入信息和预测机制上不同。

### 2.2 混合检索模型
LIGER 将语义 ID、物品位置和文本表示共同输入编码器，同时产生用户/目标物品的稠密表示。生成式候选、稠密候选和冷启动物品被合并，再依据表示相似度完成最终排序。

### 2.3 冷启动分析
论文重点研究生成式检索对未见物品的覆盖不足，并用稠密检索补充开放物品集合。该设计提升覆盖，但也意味着完整 SID 不是最终推荐的唯一决定因素。

### 2.4 实验与讨论
实验比较生成、稠密和混合方法在集合内/集合外数据上的效果，并分析 SID 输入、文本表示和候选融合的作用。

## 实验结论

- 混合检索改善了生成模型对未见物品的覆盖。
- 稠密检索与生成检索具有互补性。

## 局限性与研究定位


### 局限性
- 需要外部稠密检索和最终排序，未实现纯生成式端到端推荐。
- 多路召回与融合增加线上服务链路和延迟。


### 研究定位
LIGER 适合放在“生成式推荐与稠密检索融合”章节，而不应计入直接生成完整 SID 的核心方法主表。

## 相关资源

- 论文原文链接未报告。
- 暂未发现公开代码。

## 标签

`生成检索` · `稠密检索` · `混合检索` · `冷启动`
